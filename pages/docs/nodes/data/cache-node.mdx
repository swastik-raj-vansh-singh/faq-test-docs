---
title: Cache Node
description: Store and retrieve intermediate results in Lamatic flows to avoid redundant API calls and speed up repeated executions.
---

import { Callout } from 'nextra-theme-docs'
import SectionRows from '@/components/SectionRows'

# Cache Node

<SectionRows section="Node Type" chips={["Data"]} />

The Cache Node stores the output of an upstream node so that subsequent runs with the same input can skip expensive operations like LLM calls or external API requests and return the cached result instead.

## How It Works

1. On the first run, the Cache Node passes the request through to the downstream node and stores the response keyed by a hash of the input.
2. On later runs with identical input, it returns the stored response immediately without executing the downstream node.
3. Cached entries expire after a configurable TTL (time to live).

## Configuration

| Setting | Description | Default |
|---|---|---|
| **Cache key** | Template expression that determines what counts as "same input" | Full input hash |
| **TTL** | How long a cached entry stays valid (seconds) | 3600 (1 hour) |
| **Storage** | Where cached data is kept (in-memory or project-level persistent store) | In-memory |

<Callout type="info">
Use a narrow cache key when only part of the input matters. For example, cache by `file_path` alone if the file content rarely changes between runs.
</Callout>

## When to Use It

- Before an LLM node to avoid regenerating answers for unchanged documents.
- Before an API Node that calls a rate-limited external service.
- In loops or batch operations where the same lookup happens multiple times.

## Limitations

- In-memory caches are cleared when the flow is redeployed.
- Very large cached values increase memory usage; prefer caching references or IDs over full payloads.
- Cache invalidation is time-based only; there is no manual purge button in the current version.

## Related

See **API Node** for external calls that benefit from caching and **Code Node** for building custom cache keys.

import { FAQAccordion } from "@/components/FAQAccordion"

## Frequently Asked Questions

<FAQAccordion items={[
  {
    question: "Which cache key should I use for the Cache Node?",
    answer: "Choose a cache key that captures only the input fields that affect the downstream result. By default the Cache Node hashes the full input; override it with a template to narrow scope. For example, cache by file_path if file content rarely changes between runs. See the [Cache Node Configuration] docs for details."
  },
  {
    question: "How does Cache Node TTL work, and what value should I use?",
    answer: "TTL is the time in seconds that a cached entry remains valid before recomputation. The default is 3600 (1 hour), after which the same input triggers a fresh run and refreshes the cache. Use shorter TTLs for volatile data and longer ones for stable workloads. See the [Cache Node Limitations] section for expiration behavior."
  },
  {
    question: "When should I use a Cache Node before an LLM or API node?",
    answer: "Use a Cache Node when repeated identical inputs would re-run costly LLM or API calls. Common cases are unchanged document answers, rate-limited external services, and repeated lookups in loops or batches. Skip it when inputs change every run or freshness is required over speed. See the [API Node] docs for related patterns."
  },
  {
    question: "What’s the difference between in-memory and persistent storage for caching?",
    answer: "In-memory storage holds cache in process and is cleared on redeploy. Persistent storage records entries in a project-level store configured on the node. Pick in-memory for fast, ephemeral caching and persistent storage for project-level caching. See the [Cache Node Configuration] docs for storage options."
  },
  {
    question: "Why am I getting cache misses when I expect a hit?",
    answer: "Cache misses usually mean your key differs between runs or includes changing fields. The default full-input hash changes if any irrelevant field changes, and entries may also have expired due to TTL. In-memory caches are additionally cleared on redeploy. Narrow the key to stable fields; see [Cache Node Configuration]."
  },
  {
    question: "Can I manually invalidate or purge Cache Node entries during development?",
    answer: "No, manual purge isn’t available; cache invalidation is time-based only. Adjust the cache key or lower the TTL to force recomputation on the next run. Redeploying clears in-memory caches as a side effect. See the [Cache Node Limitations] section for details."
  },
  {
    question: "Is it possible to cache only part of the input, like file_path?",
    answer: "Yes, set the key template to the specific field you want to match. For example, use file_path so calls with the same path reuse cached results even if other inputs vary. This narrows what counts as “same input” compared to the default full-input hash. See the [Cache Node Configuration] docs for key templates."
  }
]} />
