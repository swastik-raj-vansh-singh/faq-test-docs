

import { FAQAccordion } from "@/components/FAQAccordion"

## Frequently Asked Questions

<FAQAccordion items={[
  {
    question: "Why do documents need to be split into chunks before vectorization?",
    answer: "Large documents must be split into smaller chunks because embedding models have token limits that larger texts often exceed. Chunking also ensures that retrieved segments are focused and precise, which is crucial for improving the relevance of search results in a RAG pipeline. This process prevents loss of context at boundaries by allowing configurable overlap between chunks."
  },
  {
    question: "How does the Text Splitter Node improve the relevance of RAG search results?",
    answer: "The Text Splitter Node improves RAG search relevance by breaking large documents into smaller, more focused chunks. When these smaller chunks are embedded and indexed, retrieving them leads to more precise matches with user queries. This precision is vital for providing highly relevant information rather than broad, less targeted document sections."
  },
  {
    question: "What is the role of chunk overlap in the Text Splitter Node?",
    answer: "Chunk overlap in the Text Splitter Node is essential to prevent the loss of context that might occur at the boundaries between split text segments. By including a specified number of tokens or characters from the end of one chunk at the beginning of the next, it ensures continuity. This overlap helps maintain coherence across chunks, particularly for subsequent processing like embedding and retrieval."
  },
  {
    question: "How do I integrate the Text Splitter Node into my RAG pipeline flow?",
    answer: "To integrate the Text Splitter Node, drag it from the Data section of the node palette into your flow. It should be placed after your document source, such as a File Loader Node, and positioned directly before a Vectorize Node. This placement ensures that documents are properly chunked before they are vectorized and indexed."
  },
  {
    question: "How do chunk size and chunk overlap settings impact content context?",
    answer: "Chunk size and chunk overlap settings significantly impact how much context each segment retains and how much redundancy exists. A smaller chunk size can lead to more precise retrieval but might lose broader context, while a larger size retains more context but can be less precise. Overlap ensures that no context is lost at chunk boundaries, providing continuity for embedding models."
  },
  {
    question: "How do I specify the input text for the Text Splitter Node?",
    answer: "You specify the input text for the Text Splitter Node by setting its text field to the variable containing the raw document content from an upstream node. For example, if you're using a File Loader Node, you would map the input using a reference like `{{fileLoaderNode_1.output.text}}`. This links the raw text data directly to the splitter."
  },
  {
    question: "Can I use a custom separator when splitting text with the Text Splitter Node?",
    answer: "Yes, you can use a custom separator string when splitting text with the Text Splitter Node, specifically when employing the `fixed` split strategy. This `Separator` parameter allows you to define a specific character or string that the node will use to delineate chunk boundaries. This offers precise control over how your text is segmented."
  }
]} />
